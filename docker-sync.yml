version: "2"
options:
  verbose: true
syncs:
  bitcoin-hands-on_sync:
    src: .
    sync_host_ip: 'localhost'
    sync_userid: 1000
    sync_excludes:
    - .git
    - .vscode
    - tmp
    - log
    - node_modules
